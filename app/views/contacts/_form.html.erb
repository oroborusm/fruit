<%= form_for(@contact, remote: true, validate: true, html: {id: "form-contacto", class: "form-contacto col-md-8"}) do |f| %>
  <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.text_field :name, class: 'form-control col-form-label-lg', placeholder: 'Nombre' %>
  </div>
  <div class="form-group">
    <%= f.text_field :email, class: 'form-control col-form-label-lg', placeholder: 'Correo' %>
  </div>
  <div class="form-group">
    <%= f.text_area :body, class: 'form-control col-form-label-lg', placeholder: '¿En que te podemos ayudar?' %>
  </div>
  <div class="actions submit">
    <%= f.submit "Enviar" %>
  </div>
<% end %>
